on:
  workflow_dispatch:

jobs:
  job:
    runs-on: ubuntu-latest

    steps:
      - run: ls -la ~/.ssh || true

      - uses: mxschmitt/action-tmate@v3
        with:
          limit-access-to-actor: true
      
      - run: ls -la ~/.ssh || true